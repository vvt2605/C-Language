#include <stdio.h>
int main() {
  int n, i;
  for (i = 1; i <= 10; ++i) {
    n=1;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=2;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=3;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=4;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=5;
    printf("%d * %d = %d \n", n, i, n * i);
  }
  for (i = 1; i <= 10; ++i) {
    n=6;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=7;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=8;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=9;
    printf("%d * %d = %d \n", n, i, n * i);
  }
   for (i = 1; i <= 10; ++i) {
    n=10;
    printf("%d * %d = %d \n", n, i, n * i);
  }
  return 0;
}